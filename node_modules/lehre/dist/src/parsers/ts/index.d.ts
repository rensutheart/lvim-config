import { DocProps, ParseProps } from '../../types';
export declare const getLineAndPosition: (lines: string[]) => (lineno: number) => {
    line: number;
    column: number;
};
export declare const parse: ({ code, lines, nest, scriptTarget, scriptKind, }: ParseProps) => DocProps[];
