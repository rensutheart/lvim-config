import { Expression, SourceFile } from 'typescript';
import { ParamProps } from '../../types';
export declare const getVariableDocFromExpression: (node: Expression, source: SourceFile) => {
    name: string;
    type: string;
    start: {
        line: number;
        column: number;
    };
    end: {
        line: number;
        column: number;
    };
    returnType: string;
    params: ParamProps[];
};
