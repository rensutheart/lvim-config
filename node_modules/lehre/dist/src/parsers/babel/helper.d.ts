import { Comment, File, Node } from '@babel/types';
export declare const getLocation: (node: Node) => {
    start: {
        line: number;
        column: number;
    };
    end: {
        line: number;
        column: number;
    };
};
export declare const getAst: (code: string, parser?: string) => File;
export declare const isCommentBlock: (comments: readonly Comment[]) => boolean;
