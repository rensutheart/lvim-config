import { DocProps, OutputProps } from '../types';
export declare const getFormatterPath: (formatter: string) => string;
export declare const generateDocs: (templatePath: string, docs: DocProps[]) => OutputProps[];
