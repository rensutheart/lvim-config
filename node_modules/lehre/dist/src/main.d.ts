import { CompilerOptionProps, ConfigProps, OutputProps } from './types';
declare type OutputFnProps = ({ lines, docs, filePath, }: {
    lines: string[];
    docs: OutputProps[];
    filePath: string;
}) => string;
export declare const run: (parser: 'babel' | 'ts', code: string, filePath: string, templatePath: string, nest: boolean, outputFn: OutputFnProps, options: CompilerOptionProps) => string;
export declare const main: (config: ConfigProps) => Promise<string[]>;
export {};
