import { OutputProps } from '../types';
declare type Props = {
    lines: string[];
    docs: OutputProps[];
    filePath: string;
};
export declare const stringOutputter: ({ lines, docs }: Props) => string;
export declare const jsonOutputter: ({ docs }: Props) => string;
export declare const fileOutputter: ({ lines, docs, filePath }: Props) => string;
export {};
